#include <stdio.h>
#define LEN_ARR 5

int main()
{
    int arr[LEN_ARR];
    int *ptr=arr;
    int sum=0;
    for(int i=0;i<LEN_ARR;i++)
    {
        scanf("%d",ptr+i);
    }
    //printf("value is:%d \n",*ptr+1);//(*ptr)+1 value is incremented by 1
     
     /*   printf("value is:%d \n",*(&(*ptr)+3));
          printf("value is:%p \n",&(*ptr)+1);
           printf("value is:%p \n",&(*ptr)+2);
            printf("value is:%p \n",&(*ptr)+4);
     printf("value is:%p \n",&(*ptr)+0);
   // printf("value is:%d \n",*ptr++);//increment is done  but assignment is not complte
       //printf("value is:%d ",ptr);/
        for(int i=0;i<LEN_ARR;i++)
    {
      if(i%2!=0)
      {
          sum=sum+*(ptr+i);
        
      }
   
    }
    printf("sum is:%d \n",sum);
    return 0;
    
    
}
