#include<stdio.h>
int main()
{
    int n1=0,n2=1,n,range;
    scanf("%d",&range);
    printf("%d %d ",n1,n2);
    for(int i=2;i<range;i++)
    {
        n=n1+n2;
        printf("%d ",n);
        n1=n2;
        n2=n;
    }
}
