#include <stdio.h>

int main()
{ int arr[10],n,sum=0,count;
scanf("%d",&n);
int p=n+1;
   for(int i=0;i<n;i++)
   {
    scanf("%d",&arr[i]);
    sum=sum+arr[i];
   }
  int r=(p*(p+1)/2)-sum;
  printf("%d",r);
    return 0;
}
