/*program  with fun to count sum of elements in an array and return the sum*/

#include <stdio.h>   //header file consist of printf and scanf functions
int sum_of_array(int *arr,int size)  //definition and declaration of function
{
int i,sum=0;  //declaration of int data type and intialization of sum to 0
for(i=0;i<size;i++)  //for loop to iterate over the array
{
    sum+=*(arr+i);  //logic to sum the elements in an array
}
   // printf("%d\n",sum);  //print the sum
    return sum;  //return the sum
}   
int main()       //program execution starts from here
{   int i,res;   //declaration 2 int data types
    int arr[5]={10,20,30,40,50,60};       //initialisation of  array of type int
    int size=sizeof(arr)/sizeof(int);  //find the size of array
    res=sum_of_array(arr,size);  //calling function and storing sum of elemeents in result
     printf("The sum of elements in an array is:%d",res);   //print the result by returning sum 
    return 0;   //return type
}
output:
The sum of elements in an array is:150
